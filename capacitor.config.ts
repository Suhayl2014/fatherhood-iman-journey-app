import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fatherhoodiman.app',
  appName: 'Fatherhood Iman Journey',
  webDir: 'dist'
};

export default config;
